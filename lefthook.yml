pre-push:
  jobs:
    - name: packages audit
      tags:
        - frontend
        - security
      run: bun audit

pre-commit:
  parallel: true
  jobs:
    - name: lint
      run: bun lint

    - name: format
      run: bun format
